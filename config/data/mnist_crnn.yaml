# @package _global_
data:
  dataset: mnist
  root: data
  batch_size: 256
  num_workers: 8
  shuffle_test: true
model:
  rnn_kwargs:
    batch_first: false
  num_classes: 10
layer_one_in_channels_override: 1
optimizer:
  lr: 0.005
